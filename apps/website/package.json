{
  "name": "@voltmarch/website",
  "private": true,
  "license": "UNLICENSED",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && npx wrangler pages dev dist --d1 WAITLIST=voltmarch-launch-waitlist",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name voltmarch-coming-soon",
    "test": "node --test test/*.spec.mjs",
    "test:contracts": "npm test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
