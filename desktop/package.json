{
  "name": "voltmarch-desktop",
  "private": true,
  "description": "VOLTMARCH desktop shell. Wraps the UNMODIFIED web build in Electron so the discrete GPU can be enforced and a match survives alt-tab.",
  "main": "out/main.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "npm run build && electron .",
    "dist": "npm run build && node dist.mjs"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "^0.25.0"
  }
}
